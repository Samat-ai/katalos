'use client';
import { useState } from 'react';
import type { TasteProfile } from '@/lib/taste/schema';
export function TasteProfilerCard({ username, publicCount }: { username: string; publicCount?: number }) { const [profile, setProfile] = useState<TasteProfile | null>(null); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); const read = async () => { setLoading(true); setError(''); try { const r = await fetch('/api/taste-profile', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ username }) }); const b = await r.json(); if (!r.ok) throw new Error(b.error); setProfile(b.profile); } catch (e) { setError(e instanceof Error ? e.message : 'The profiler is napping. Try again — the room works fine without it.'); } finally { setLoading(false); } }; return profile ? <section className="taste-card"><p className="eyebrow">Powered by Gemini · public entries only</p><h2>{profile.archetype}</h2><p>{profile.profile}</p><ul>{profile.signals.map((x) => <li key={x}>{x}</li>)}</ul><p><strong>First pick: {profile.firstPick.title}</strong> — {profile.firstPick.reason}</p></section> : <section className="taste-card"><p className="eyebrow">Powered by Gemini · public entries only</p><h2>WHAT DOES THIS ROOM SAY?</h2>{publicCount !== undefined && publicCount < 3 ? <p>Shelve a few more things first.</p> : <>{error && <p role="alert">{error}</p>}<button disabled={loading} onClick={() => void read()}>{loading ? 'CONSULTING THE SHELVES…' : error ? 'TRY AGAIN' : 'READ MY TASTE'}</button></>}</section>; }
