'use client';
import { useState } from 'react';
import type { TasteProfile } from '@/lib/taste/schema';
export function TasteProfilerCard({ username }: { username: string }) { const [profile, setProfile] = useState<TasteProfile | null>(null); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); const read = async () => { setLoading(true); setError(''); try { const r = await fetch('/api/taste-profile', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ username }) }); const b = await r.json(); if (!r.ok) throw new Error(b.error); setProfile(b.profile); } catch (e) { setError(e instanceof Error ? e.message : 'The profiler is taking a tea break. Please retry.'); } finally { setLoading(false); } }; return profile ? <section className="taste-card"><h2>{profile.archetype}</h2><p>{profile.profile}</p><ul>{profile.signals.map((x) => <li key={x}>{x}</li>)}</ul><p><strong>First pick: {profile.firstPick.title}</strong> — {profile.firstPick.reason}</p></section> : <section className="taste-card"><h2>What does this room say?</h2>{error && <p role="alert">{error}</p>}<button disabled={loading} onClick={() => void read()}>{loading ? 'Reading the room…' : error ? 'Retry' : 'Read my taste'}</button></section>; }
